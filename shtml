<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Simpson Store — Juguetes y Coleccionables</title>
<link href="https://fonts.googleapis.com/css2?family=Bangers&family=Nunito:wght@400;600;700;800;900&display=swap" rel="stylesheet">
<style>
:root{
  --yellow:#FED90F;--yellow2:#FFE55A;--yellow-dark:#D4A800;
  --blue:#1A6FB5;--blue2:#2589D4;--sky:#87CEEB;
  --dark:#1a1a1a;--white:#FAFAF5;--gray:#f0ede4;
  --red:#E32B2B;--green:#27A05A;--muted:#555;
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{background:var(--white);color:var(--dark);font-family:'Nunito',sans-serif;overflow-x:hidden}

/* SKY */
body::before{content:'';position:fixed;top:0;left:0;right:0;height:100vh;background:linear-gradient(180deg,#87CEEB 0%,#c8e8f7 35%,var(--white) 75%);z-index:-2;opacity:.2}

/* HEADER */
header{position:sticky;top:0;z-index:200;background:var(--yellow);border-bottom:4px solid var(--dark);display:flex;align-items:center;justify-content:space-between;padding:.7rem 2rem;box-shadow:0 4px 0 rgba(0,0,0,.12);gap:1rem}
.logo{font-family:'Bangers',cursive;font-size:2rem;letter-spacing:.08em;color:var(--dark);text-decoration:none;flex-shrink:0}
.logo span{color:var(--blue)}
.search-bar{flex:1;max-width:420px;position:relative}
.search-bar input{width:100%;background:rgba(255,255,255,.85);border:2.5px solid var(--dark);border-radius:30px;padding:.5rem 1rem .5rem 2.4rem;font-family:'Nunito',sans-serif;font-size:.88rem;font-weight:700;outline:none;transition:border-color .2s,background .2s}
.search-bar input:focus{background:white;border-color:var(--blue)}
.search-bar svg{position:absolute;left:.8rem;top:50%;transform:translateY(-50%);opacity:.5;pointer-events:none}
.hright{display:flex;align-items:center;gap:.6rem;flex-shrink:0}
nav a{font-weight:800;font-size:.8rem;letter-spacing:.05em;text-transform:uppercase;color:var(--dark);text-decoration:none;margin-left:1.2rem;transition:color .2s}
nav a:hover{color:var(--blue)}
.cart-btn{background:var(--blue);border:3px solid var(--dark);color:white;padding:.5rem 1.1rem;border-radius:30px;cursor:pointer;font-family:'Nunito',sans-serif;font-weight:900;font-size:.85rem;display:flex;align-items:center;gap:.45rem;box-shadow:3px 3px 0 var(--dark);transition:transform .15s,box-shadow .15s}
.cart-btn:hover{transform:translate(-1px,-1px);box-shadow:4px 4px 0 var(--dark)}
.cc{background:var(--red);color:white;border-radius:50%;width:19px;height:19px;display:flex;align-items:center;justify-content:center;font-size:.65rem;font-weight:900;border:2px solid white}

/* HERO */
.hero{display:grid;grid-template-columns:1fr 1fr;min-height:85vh;border-bottom:4px solid var(--dark);overflow:hidden;position:relative}
.hero-l{display:flex;flex-direction:column;justify-content:center;padding:4rem 3rem 4rem 4rem;z-index:2}
.hero-tag{display:inline-flex;align-items:center;gap:.4rem;background:var(--blue);color:white;font-weight:900;font-size:.72rem;letter-spacing:.14em;text-transform:uppercase;padding:.32rem .9rem;border-radius:30px;border:2px solid var(--dark);box-shadow:2px 2px 0 var(--dark);margin-bottom:1.5rem;width:fit-content}
.hero h1{font-family:'Bangers',cursive;font-size:clamp(3.5rem,6vw,6.5rem);line-height:.95;letter-spacing:.04em;margin-bottom:1rem}
.hero h1 .y{color:var(--yellow);-webkit-text-stroke:3px var(--dark)}
.hero h1 .b{color:var(--blue)}
.hero-sub{font-size:1rem;line-height:1.65;color:var(--muted);font-weight:600;max-width:400px;margin-bottom:2rem}
.hero-cta{display:flex;gap:1rem;align-items:center;flex-wrap:wrap}
.btn-main{background:var(--yellow);border:3px solid var(--dark);color:var(--dark);padding:.85rem 2rem;border-radius:30px;font-family:'Nunito',sans-serif;font-weight:900;font-size:.95rem;cursor:pointer;text-decoration:none;box-shadow:4px 4px 0 var(--dark);transition:transform .15s,box-shadow .15s;display:inline-block}
.btn-main:hover{transform:translate(-2px,-2px);box-shadow:6px 6px 0 var(--dark)}
.btn-out{background:transparent;border:3px solid var(--dark);color:var(--dark);padding:.85rem 1.5rem;border-radius:30px;font-family:'Nunito',sans-serif;font-weight:800;font-size:.95rem;cursor:pointer;text-decoration:none;transition:background .2s;display:inline-block}
.btn-out:hover{background:rgba(0,0,0,.06)}
.hero-r{background:linear-gradient(155deg,#FFE566 0%,var(--yellow) 55%,#FFB800 100%);border-left:4px solid var(--dark);display:flex;align-items:center;justify-content:center;position:relative;overflow:hidden}
.hero-r::before{content:'';position:absolute;inset:0;background-image:radial-gradient(rgba(0,0,0,.055) 1px,transparent 1px);background-size:26px 26px}
.hero-chars{position:relative;z-index:2;display:flex;gap:1.5rem;align-items:flex-end}
.hchar{font-size:5rem;animation:hbounce 3s ease-in-out infinite;filter:drop-shadow(4px 6px 0 rgba(0,0,0,.2))}
.hchar:nth-child(2){animation-delay:.4s;font-size:4.2rem}
.hchar:nth-child(3){animation-delay:.8s;font-size:4.6rem}
@keyframes hbounce{0%,100%{transform:translateY(0) rotate(-3deg)}50%{transform:translateY(-18px) rotate(3deg)}}
.fb{position:absolute;background:white;border:3px solid var(--dark);border-radius:10px;padding:.55rem 1rem;font-weight:900;font-size:.82rem;box-shadow:3px 3px 0 var(--dark);animation:fbf 4s ease-in-out infinite}
.fb .n{font-family:'Bangers',cursive;font-size:1.4rem;color:var(--blue);display:block;line-height:1}
.fb1{top:16%;right:10%;animation-delay:0s}
.fb2{bottom:18%;left:7%;animation-delay:2s}
@keyframes fbf{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}

/* LOADING STATE */
.loading-state{display:flex;flex-direction:column;align-items:center;justify-content:center;padding:5rem 2rem;gap:1.5rem;grid-column:1/-1}
.spinner{width:48px;height:48px;border:4px solid rgba(26,111,181,.2);border-top-color:var(--blue);border-radius:50%;animation:spin 1s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.loading-text{font-family:'Bangers',cursive;font-size:1.6rem;letter-spacing:.06em;color:var(--blue)}

/* ERROR STATE */
.error-state{grid-column:1/-1;text-align:center;padding:4rem 2rem;background:white;border:3px solid var(--dark);border-radius:16px;box-shadow:4px 4px 0 var(--dark)}
.error-state h3{font-family:'Bangers',cursive;font-size:2rem;color:var(--red);margin-bottom:.5rem}
.error-state p{color:var(--muted);font-size:.9rem;line-height:1.6}

/* PERKS */
.perks{display:flex;border-bottom:4px solid var(--dark);background:white}
.perk{flex:1;display:flex;align-items:center;gap:.8rem;padding:1.1rem 1.5rem;border-right:3px solid var(--dark)}
.perk:last-child{border-right:none}
.pi{width:36px;height:36px;background:var(--yellow);border:2px solid var(--dark);border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:1rem;flex-shrink:0;box-shadow:2px 2px 0 var(--dark)}
.pt strong{display:block;font-size:.82rem;font-weight:900}
.pt span{font-size:.72rem;color:var(--muted);font-weight:600}

/* PRODUCTS */
.prods{padding:4rem 2rem;max-width:1400px;margin:0 auto}
.sec-hd{display:flex;align-items:flex-end;justify-content:space-between;margin-bottom:1.75rem;flex-wrap:wrap;gap:1rem}
.sec-title{font-family:'Bangers',cursive;font-size:2.8rem;letter-spacing:.05em}
.rcount{font-size:.82rem;color:var(--muted);font-weight:700}
.fbar{display:flex;gap:.45rem;flex-wrap:wrap;margin-bottom:2rem}
.fbt{background:white;border:2.5px solid var(--dark);color:var(--dark);padding:.38rem 1rem;border-radius:30px;cursor:pointer;font-family:'Nunito',sans-serif;font-size:.78rem;font-weight:800;box-shadow:2px 2px 0 rgba(0,0,0,.12);transition:all .2s}
.fbt:hover,.fbt.on{background:var(--yellow);box-shadow:2px 2px 0 var(--dark)}

/* GRID */
.pgrid{display:grid;grid-template-columns:repeat(auto-fill,minmax(255px,1fr));gap:1.25rem}
.pc{background:white;border:3px solid var(--dark);border-radius:14px;overflow:hidden;transition:transform .2s,box-shadow .2s;box-shadow:4px 4px 0 var(--dark);position:relative;animation:popIn .4s cubic-bezier(.34,1.56,.64,1) both}
.pc:hover{transform:translate(-3px,-3px);box-shadow:7px 7px 0 var(--dark)}
@keyframes popIn{from{opacity:0;transform:scale(.87)}to{opacity:1;transform:scale(1)}}
.pbadge{position:absolute;top:10px;left:10px;font-weight:900;font-size:.65rem;letter-spacing:.08em;text-transform:uppercase;padding:.28rem .65rem;border-radius:30px;border:2px solid var(--dark);z-index:3;box-shadow:2px 2px 0 rgba(0,0,0,.15)}
.pbadge.hot{background:var(--red);color:white}
.pbadge.new{background:var(--green);color:white}
.pbadge.pop{background:var(--blue);color:white}
.pimg{height:220px;background:var(--yellow);border-bottom:3px solid var(--dark);display:flex;align-items:center;justify-content:center;overflow:hidden;position:relative}
.pimg::before{content:'';position:absolute;inset:0;background-image:radial-gradient(rgba(0,0,0,.05) 1px,transparent 1px);background-size:18px 18px}
.pimg img{width:100%;height:100%;object-fit:contain;padding:.75rem;position:relative;z-index:1;transition:transform .35s}
.pc:hover .pimg img{transform:scale(1.08) rotate(2deg)}
.pimg .pemoji{font-size:4rem;position:relative;z-index:1;transition:transform .35s}
.pc:hover .pemoji{transform:scale(1.1) rotate(3deg)}
.pinfo{padding:1rem}
.pcat{font-size:.65rem;font-weight:800;letter-spacing:.12em;text-transform:uppercase;color:var(--blue);margin-bottom:.28rem}
.pname{font-family:'Bangers',cursive;font-size:1.25rem;letter-spacing:.04em;line-height:1.15;margin-bottom:.22rem;color:var(--dark)}
.pdesc{font-size:.76rem;color:var(--muted);line-height:1.5;margin-bottom:.9rem;font-weight:600;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
.pfoot{display:flex;align-items:center;justify-content:space-between;gap:.5rem}
.pprice{font-family:'Bangers',cursive;font-size:1.9rem;letter-spacing:.03em;color:var(--dark)}
.pprice .ps{font-family:'Nunito',sans-serif;font-size:.82rem;color:var(--blue);font-weight:900;vertical-align:super;margin-right:1px}
.pbtns{display:flex;gap:.4rem}
.btn-ml{background:white;border:2px solid var(--dark);color:var(--dark);padding:.45rem .7rem;border-radius:30px;cursor:pointer;font-size:.68rem;font-weight:800;text-decoration:none;display:flex;align-items:center;gap:.3rem;transition:all .2s;white-space:nowrap;box-shadow:2px 2px 0 rgba(0,0,0,.1)}
.btn-ml:hover{background:var(--gray);border-color:var(--blue);color:var(--blue)}
.btn-cart{background:var(--yellow);border:2.5px solid var(--dark);color:var(--dark);padding:.45rem .9rem;border-radius:30px;cursor:pointer;font-family:'Nunito',sans-serif;font-weight:900;font-size:.75rem;box-shadow:2px 2px 0 var(--dark);transition:transform .15s,box-shadow .15s,background .15s;white-space:nowrap}
.btn-cart:hover{background:var(--yellow2);transform:translate(-1px,-1px);box-shadow:3px 3px 0 var(--dark)}
.btn-cart:active{transform:translate(1px,1px);box-shadow:0 0 0 var(--dark)}

/* CART */
.cart-overlay{position:fixed;inset:0;background:rgba(0,0,0,.55);backdrop-filter:blur(4px);z-index:300;opacity:0;pointer-events:none;transition:opacity .3s}
.cart-overlay.open{opacity:1;pointer-events:all}
.cart-sidebar{position:fixed;top:0;right:0;width:min(400px,100vw);height:100vh;background:var(--white);border-left:4px solid var(--dark);z-index:301;display:flex;flex-direction:column;transform:translateX(100%);transition:transform .35s cubic-bezier(.4,0,.2,1);box-shadow:-6px 0 0 rgba(0,0,0,.08)}
.cart-sidebar.open{transform:translateX(0)}
.cart-head{background:var(--yellow);border-bottom:4px solid var(--dark);display:flex;align-items:center;justify-content:space-between;padding:1.2rem 1.5rem}
.cart-head-title{font-family:'Bangers',cursive;font-size:1.9rem;letter-spacing:.06em}
.cart-close{background:white;border:2px solid var(--dark);color:var(--dark);width:32px;height:32px;border-radius:50%;cursor:pointer;font-size:1.1rem;display:flex;align-items:center;justify-content:center;box-shadow:2px 2px 0 var(--dark);transition:transform .15s}
.cart-close:hover{transform:rotate(10deg)}
.cart-body{flex:1;overflow-y:auto;padding:1rem 1.25rem;display:flex;flex-direction:column;gap:.7rem}
.cart-empty-msg{display:flex;flex-direction:column;align-items:center;justify-content:center;height:100%;gap:.75rem;color:#aaa;font-weight:700;text-align:center}
.cart-empty-msg .ei{font-size:3.5rem}
.ci{background:white;border:2.5px solid var(--dark);border-radius:10px;padding:.7rem;display:flex;gap:.75rem;align-items:center;box-shadow:2px 2px 0 var(--dark);animation:popIn .3s ease}
.ci-img{width:52px;height:52px;background:var(--yellow);border:2px solid var(--dark);border-radius:6px;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:1.8rem;overflow:hidden}
.ci-img img{width:100%;height:100%;object-fit:contain}
.ci-info{flex:1;min-width:0}
.ci-name{font-weight:800;font-size:.86rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ci-price{font-weight:900;color:var(--blue);font-size:.84rem;margin-top:2px}
.ci-ctrl{display:flex;align-items:center;gap:.35rem}
.qb{background:var(--gray);border:2px solid var(--dark);color:var(--dark);width:27px;height:27px;border-radius:50%;cursor:pointer;font-size:1.1rem;display:flex;align-items:center;justify-content:center;font-weight:900;transition:background .15s}
.qb:hover{background:var(--yellow)}
.qn{font-weight:900;min-width:20px;text-align:center;font-size:.88rem}
.cart-foot{padding:1rem 1.25rem 1.5rem;border-top:3px solid var(--dark);background:white}
.total-row{display:flex;justify-content:space-between;align-items:center;margin-bottom:1rem}
.total-lbl{font-weight:800;color:var(--muted);font-size:.85rem;text-transform:uppercase;letter-spacing:.06em}
.total-amt{font-family:'Bangers',cursive;font-size:2.2rem;letter-spacing:.04em}
.chk-btn{width:100%;background:var(--blue);border:3px solid var(--dark);color:white;padding:.9rem;border-radius:30px;cursor:pointer;font-family:'Nunito',sans-serif;font-weight:900;font-size:.95rem;box-shadow:4px 4px 0 var(--dark);transition:transform .15s,box-shadow .15s;margin-bottom:.6rem}
.chk-btn:hover{transform:translate(-2px,-2px);box-shadow:6px 6px 0 var(--dark)}
.chk-btn:disabled{background:#ccc;border-color:#aaa;cursor:not-allowed;box-shadow:none;transform:none}
.clr-btn{width:100%;background:transparent;border:2px solid #ccc;color:#aaa;padding:.58rem;border-radius:30px;cursor:pointer;font-family:'Nunito',sans-serif;font-weight:700;font-size:.82rem;transition:all .2s}
.clr-btn:hover{border-color:var(--red);color:var(--red)}

/* TOAST */
.toast{position:fixed;bottom:2rem;left:50%;transform:translateX(-50%) translateY(80px);background:var(--dark);color:var(--yellow);border:3px solid var(--yellow);padding:.75rem 1.5rem;border-radius:30px;font-weight:900;font-size:.88rem;z-index:400;transition:transform .3s cubic-bezier(.34,1.56,.64,1);white-space:nowrap;box-shadow:4px 4px 0 rgba(0,0,0,.3)}
.toast.show{transform:translateX(-50%) translateY(0)}

/* FOOTER */
footer{background:var(--dark);color:var(--yellow);border-top:4px solid var(--dark);padding:2.5rem 2rem;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:1rem}
.f-logo{font-family:'Bangers',cursive;font-size:1.8rem;letter-spacing:.08em;color:var(--yellow);text-decoration:none}
.f-logo span{color:var(--blue)}
footer p{font-size:.78rem;color:rgba(254,217,15,.55);font-weight:600}
footer a{color:var(--yellow)}

/* RESPONSIVE */
@media(max-width:860px){
  header{padding:.6rem 1rem}
  nav{display:none}
  .search-bar{max-width:none;flex:1}
  .hero{grid-template-columns:1fr;min-height:auto}
  .hero-l{padding:2.5rem 1.5rem}
  .hero-r{height:260px;border-left:none;border-top:4px solid var(--dark)}
  .perks{flex-wrap:wrap}
  .perk{flex:1 1 48%;border-bottom:3px solid var(--dark)}
  .prods{padding:3rem 1rem}
  .sec-hd{flex-direction:column;align-items:flex-start}
  footer{flex-direction:column;text-align:center;padding:2rem 1rem}
}
@media(max-width:480px){
  .hero h1{font-size:3rem}
}
</style>
</head>
<body>

<!-- HEADER -->
<header>
  <a href="#" class="logo">Simpson<span>Store</span></a>
  <div class="search-bar">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
    <input type="text" id="searchInput" placeholder="Buscar juguetes, figuras, coleccionables…" oninput="handleSearch()">
  </div>
  <div class="hright">
    <nav>
      <a href="#productos">Catálogo</a>
      <a href="#contacto">Contacto</a>
    </nav>
    <button class="cart-btn" onclick="toggleCart()">🛒 Carrito <span class="cc" id="cartCount">0</span></button>
  </div>
</header>

<!-- HERO -->
<section class="hero">
  <div class="hero-l">
    <span class="hero-tag">⭐ Coleccionables Oficiales</span>
    <h1>Juguetes<br><span class="y">Los</span><br><span class="b">Simpsons</span></h1>
    <p class="hero-sub">Figuras, coleccionables, juguetes y más. Todo de tus personajes favoritos de Springfield. ¡D'oh!</p>
    <div class="hero-cta">
      <a href="#productos" class="btn-main">¡Ver Colección!</a>
      <a href="#contacto" class="btn-out">Contactar →</a>
    </div>
  </div>
  <div class="hero-r">
    <div class="hero-chars">
      <span class="hchar">🟡</span>
      <span class="hchar">🍩</span>
      <span class="hchar">📺</span>
    </div>
    <div class="fb fb1"><span class="n">+200</span>Productos</div>
    <div class="fb fb2"><span class="n">100%</span>Originales</div>
  </div>
</section>

<!-- PERKS -->
<div class="perks">
  <div class="perk"><div class="pi">🚚</div><div class="pt"><strong>Envío gratis</strong><span>A todo México</span></div></div>
  <div class="perk"><div class="pi">✅</div><div class="pt"><strong>Compra segura</strong><span>Via Mercado Libre</span></div></div>
  <div class="perk"><div class="pi">⭐</div><div class="pt"><strong>Originales</strong><span>100% auténticos</span></div></div>
  <div class="perk"><div class="pi">🎁</div><div class="pt"><strong>Regalo ideal</strong><span>Para fans de los Simpsons</span></div></div>
</div>

<!-- PRODUCTOS -->
<section class="prods" id="productos">
  <div class="sec-hd">
    <div>
      <h2 class="sec-title">🛒 Nuestra Colección</h2>
    </div>
    <span class="rcount" id="rcount"></span>
  </div>
  <div class="fbar" id="filterBar">
    <button class="fbt on" onclick="setFilter('todos',this)">Todos</button>
  </div>
  <div class="pgrid" id="pgrid">
    <div class="loading-state">
      <div class="spinner"></div>
      <p class="loading-text">Cargando productos desde Mercado Libre…</p>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer id="contacto">
  <a href="#" class="f-logo">Simpson<span>Store</span></a>
  <p>📦 Todos los productos via <a href="https://www.mercadolibre.com.mx" target="_blank">Mercado Libre</a></p>
  <p>© 2025 SimpsonStore.com — Todos los derechos reservados</p>
</footer>

<!-- CARRITO -->
<div class="cart-overlay" id="cartOverlay" onclick="toggleCart()"></div>
<aside class="cart-sidebar" id="cartSidebar">
  <div class="cart-head">
    <span class="cart-head-title">🛒 Mi Carrito</span>
    <button class="cart-close" onclick="toggleCart()">✕</button>
  </div>
  <div class="cart-body" id="cartBody"></div>
  <div class="cart-foot">
    <div class="total-row">
      <span class="total-lbl">Total</span>
      <span class="total-amt" id="cartTotal">$0</span>
    </div>
    <button class="chk-btn" id="chkBtn" onclick="checkout()" disabled>Comprar por WhatsApp 🛒</button>
    <button class="clr-btn" onclick="clearCart()">Vaciar carrito</button>
  </div>
</aside>
<div class="toast" id="toast"></div>

<script>
/* ═══════════════════════════════════════════════════
   CONFIGURACIÓN — reemplaza con tus datos reales
   ═══════════════════════════════════════════════════ */
const CONFIG = {
  CLIENT_ID:     "2985587398780518",
  CLIENT_SECRET: "bKtCCI8gsQGq56Ea2j6Dctph3wCbGvcQ",
  SELLER_ID:     "343604045",          // tu ID de vendedor en ML
  WA_NUMBER:     "521XXXXXXXXXX",      // tu WhatsApp: 521 + 10 dígitos
  ML_SITE:       "MLM",
  PROXY:         "https://ml-proxy.aulois94.workers.dev",
};

/* ═══════════════════════════════════════════════════
   ESTADO
   ═══════════════════════════════════════════════════ */
let allProducts = [];
let cart = JSON.parse(localStorage.getItem('ss_cart') || '[]');
let activeFilter = 'todos';
let searchQ = '';
let accessToken = null;
let tokenExpiry = 0;

/* ═══════════════════════════════════════════════════
   ML API — obtener token y productos
   ═══════════════════════════════════════════════════ */
async function getToken() {
  if (accessToken && Date.now() < tokenExpiry) return accessToken;
  try {
    const res = await fetch(`${CONFIG.PROXY}/oauth/token`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: new URLSearchParams({
        grant_type:    'client_credentials',
        client_id:     CONFIG.CLIENT_ID,
        client_secret: CONFIG.CLIENT_SECRET,
      })
    });
    const data = await res.json();
    if (data.access_token) {
      accessToken = data.access_token;
      tokenExpiry = Date.now() + (data.expires_in - 60) * 1000;
      return accessToken;
    }
  } catch(e) { console.error('Token error:', e); }
  return null;
}

async function fetchProducts() {
  const token = await getToken();
  if (!token) { showError('No se pudo conectar con Mercado Libre. Verifica las credenciales.'); return; }

  try {
    // Trae hasta 200 productos del vendedor
    const searchRes = await fetch(
      `${CONFIG.PROXY}/sites/${CONFIG.ML_SITE}/search?seller_id=${CONFIG.SELLER_ID}&limit=200`,
      { headers: { Authorization: `Bearer ${token}` } }
    );
    const searchData = await searchRes.json();
    const items = searchData.results || [];

    if (!items.length) { showError('No se encontraron productos. Verifica tu Seller ID.'); return; }

    // Enriquecer con detalles de cada item (foto, descripción, etc.)
    const CHUNK = 20;
    const detailed = [];
    for (let i = 0; i < Math.min(items.length, 200); i += CHUNK) {
      const chunk = items.slice(i, i + CHUNK);
      const ids = chunk.map(p => p.id).join(',');
      const detRes = await fetch(
        `${CONFIG.PROXY}/items?ids=${ids}&attributes=id,title,price,thumbnail,pictures,permalink,category_id,condition,available_quantity`,
        { headers: { Authorization: `Bearer ${token}` } }
      );
      const detData = await detRes.json();
      detData.forEach(r => { if (r.code === 200) detailed.push(r.body); });
    }

    allProducts = detailed.map(p => ({
      id:       p.id,
      name:     p.title,
      price:    p.price,
      img:      p.pictures?.[0]?.url || p.thumbnail?.replace(/I_/,'F_') || p.thumbnail,
      ml:       p.permalink,
      cat:      p.category_id || 'general',
      stock:    p.available_quantity,
      cond:     p.condition,
    }));

    buildFilters();
    render();
  } catch(e) {
    console.error(e);
    showError('Error al cargar productos. Intenta recargar la página.');
  }
}

function showError(msg) {
  document.getElementById('pgrid').innerHTML = `
    <div class="error-state">
      <h3>¡Ay, caramba!</h3>
      <p>${msg}</p>
      <button class="btn-main" style="margin-top:1.5rem" onclick="fetchProducts()">Reintentar</button>
    </div>`;
}

/* ═══════════════════════════════════════════════════
   FILTROS DINÁMICOS
   ═══════════════════════════════════════════════════ */
const CAT_LABELS = {
  'MLM1132': '🎮 Juguetes',
  'MLM1586': '🧸 Peluches',
  'MLM1500': '🎲 Juegos',
  'general': '📦 General',
};

function buildFilters() {
  const cats = [...new Set(allProducts.map(p => p.cat))];
  const bar = document.getElementById('filterBar');
  bar.innerHTML = `<button class="fbt on" onclick="setFilter('todos',this)">Todos (${allProducts.length})</button>`;
  cats.forEach(c => {
    const count = allProducts.filter(p => p.cat === c).length;
    const label = CAT_LABELS[c] || '📦 ' + c;
    bar.innerHTML += `<button class="fbt" onclick="setFilter('${c}',this)">${label} (${count})</button>`;
  });
}

/* ═══════════════════════════════════════════════════
   RENDER PRODUCTOS
   ═══════════════════════════════════════════════════ */
function render() {
  const grid = document.getElementById('pgrid');
  const q = searchQ.toLowerCase();
  const list = allProducts.filter(p => {
    const catOk = activeFilter === 'todos' || p.cat === activeFilter;
    const srchOk = !q || p.name.toLowerCase().includes(q);
    return catOk && srchOk;
  });

  document.getElementById('rcount').textContent = list.length + ' producto' + (list.length !== 1 ? 's' : '');

  if (!list.length) {
    grid.innerHTML = `<div style="grid-column:1/-1;text-align:center;padding:4rem;color:#aaa">
      <div style="font-size:2.5rem;margin-bottom:.75rem">🔍</div>
      <p style="font-weight:700">Sin resultados. Intenta con otro término.</p></div>`;
    return;
  }

  grid.innerHTML = list.map((p, i) => {
    const delay = Math.min(i * .05, .6).toFixed(2);
    const condBadge = p.cond === 'new' ? '<span class="pbadge new">Nuevo</span>' : '';
    const outOfStock = p.stock === 0 ? '<span class="pbadge hot">Agotado</span>' : '';
    const imgHtml = p.img
      ? `<img src="${p.img}" alt="${p.name}" loading="lazy">`
      : `<span class="pemoji">🟡</span>`;
    const addBtn = p.stock > 0
      ? `<button class="btn-cart" onclick="addToCart('${p.id}')">+ Carrito</button>`
      : `<button class="btn-cart" disabled style="opacity:.4;cursor:not-allowed">Agotado</button>`;
    return `<div class="pc" style="animation-delay:${delay}s">
      ${condBadge}${outOfStock}
      <div class="pimg">${imgHtml}</div>
      <div class="pinfo">
        <p class="pcat">${CAT_LABELS[p.cat] || '📦 Coleccionable'}</p>
        <h3 class="pname">${p.name}</h3>
        <div class="pfoot">
          <div class="pprice"><span class="ps">$</span>${p.price.toLocaleString('es-MX')}</div>
          <div class="pbtns">
            <a class="btn-ml" href="${p.ml}" target="_blank" rel="noopener">
              Ver ML
              <svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
            </a>
            ${addBtn}
          </div>
        </div>
      </div>
    </div>`;
  }).join('');
}

function setFilter(cat, btn) {
  activeFilter = cat;
  document.querySelectorAll('.fbt').forEach(b => b.classList.remove('on'));
  btn.classList.add('on');
  render();
}

function handleSearch() {
  searchQ = document.getElementById('searchInput').value;
  render();
}

/* ═══════════════════════════════════════════════════
   CARRITO
   ═══════════════════════════════════════════════════ */
function save() { localStorage.setItem('ss_cart', JSON.stringify(cart)); }
function getTotal() { return cart.reduce((s,i) => { const p = allProducts.find(p=>p.id===i.id); return s + (p ? p.price * i.qty : 0); }, 0); }
function getCount() { return cart.reduce((s,i) => s + i.qty, 0); }
function fmt(n) { return '$' + n.toLocaleString('es-MX'); }

function addToCart(id) {
  const p = allProducts.find(p => p.id === id);
  const ex = cart.find(i => i.id === id);
  if (ex) ex.qty++;
  else cart.push({ id, qty: 1 });
  save(); updateCartUI();
  showToast('✓ ' + p.name.substring(0,30) + '… agregado');
}

function updateQty(id, d) {
  const item = cart.find(i => i.id === id);
  if (!item) return;
  item.qty += d;
  if (item.qty <= 0) cart = cart.filter(i => i.id !== id);
  save(); updateCartUI();
}

function clearCart() { cart = []; save(); updateCartUI(); }

function updateCartUI() {
  document.getElementById('cartCount').textContent = getCount();
  document.getElementById('cartTotal').textContent = fmt(getTotal());
  document.getElementById('chkBtn').disabled = cart.length === 0;
  const body = document.getElementById('cartBody');
  if (!cart.length) {
    body.innerHTML = `<div class="cart-empty-msg"><div class="ei">🛒</div><p>Tu carrito está vacío</p><p style="font-size:.78rem;color:#ccc;font-weight:600">Agrega productos para continuar</p></div>`;
    return;
  }
  body.innerHTML = cart.map(item => {
    const p = allProducts.find(p => p.id === item.id);
    if (!p) return '';
    const imgHtml = p.img ? `<img src="${p.img}" alt="${p.name}">` : `<span style="font-size:1.8rem">🟡</span>`;
    return `<div class="ci">
      <div class="ci-img">${imgHtml}</div>
      <div class="ci-info">
        <div class="ci-name">${p.name.substring(0,40)}${p.name.length>40?'…':''}</div>
        <div class="ci-price">${fmt(p.price)}</div>
      </div>
      <div class="ci-ctrl">
        <button class="qb" onclick="updateQty('${p.id}',-1)">−</button>
        <span class="qn">${item.qty}</span>
        <button class="qb" onclick="updateQty('${p.id}',1)">+</button>
      </div>
    </div>`;
  }).join('');
}

function toggleCart() {
  document.getElementById('cartOverlay').classList.toggle('open');
  document.getElementById('cartSidebar').classList.toggle('open');
}

function checkout() {
  const lines = cart.map(i => {
    const p = allProducts.find(p => p.id === i.id);
    return `• ${p.name} x${i.qty} = ${fmt(p.price * i.qty)}`;
  }).join('\n');
  const msg = `¡Hola! Me interesa comprar los siguientes productos de Simpson Store:\n\n${lines}\n\n💰 Total: ${fmt(getTotal())}\n\n¿Tienen disponibilidad?`;
  window.open(`https://wa.me/${CONFIG.WA_NUMBER}?text=${encodeURIComponent(msg)}`, '_blank');
}

/* ═══════════════════════════════════════════════════
   TOAST
   ═══════════════════════════════════════════════════ */
let tt;
function showToast(msg) {
  const el = document.getElementById('toast');
  el.textContent = msg; el.classList.add('show');
  clearTimeout(tt); tt = setTimeout(() => el.classList.remove('show'), 2600);
}

/* ═══════════════════════════════════════════════════
   INIT — carga productos al abrir la página
   ═══════════════════════════════════════════════════ */
fetchProducts();
updateCartUI();
</script>
</body>
</html>
